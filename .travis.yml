language: go
sudo: required
install: true
env:
  global:
    - GOARCH=amd64
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install nvidia-cuda-toolkit nvidia-cuda-dev -y
script:
  - go build ./...
deploy:
  skip_cleanup: true